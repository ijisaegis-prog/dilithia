# Provenance and Reuse Audit

Historical inspections and hash/history statements below are
**EXECUTOR-REPORTED OBSERVATIONS** unless a cited tracked source directly states
the disposition. They are not upgraded by repetition in this run.

## Current Gate-1 package

The executor reported that checked working-tree files matched their Git blobs
and that four frozen inputs matched identities embedded in structural evidence. The
current five evidence files are also tracked and content-identifiable, but their
own manifest deliberately records that the earlier package identities did not
bind the current five-file package or the applicable governing set in one
pre-result durable receipt. Later tracking cannot retroactively satisfy that
method. Disposition: **inspectable structural draft; not eligible for direct
Gate-1 credit; re-execution required**.

The executor reported this history sequence: common case `bf1a84d...`; manifest `0522abbd...`;
mappings `ed517175...`; binding `3984d8a9...`; reviewed evidence package
`9bc137ef...`; planning package `e4e80804...`.

## Older pilot

The executor reported pilot history from mappings/methodology through Phase-B publication
(`e8490bf...`, `d3322cb...`, `807fb1f...`, `c1573e9...`, `6d28b4a...`). The
repository's focused reuse assessment identifies material mismatch in common
case/profile/mapping identity and limits reuse to procedure patterns. No
qualifying artifact is credited here as removing those mismatches. Disposition: **NOT ELIGIBLE**
for `G1-AUTH-001` or present Gate credit.

## Reuse rule applied as a documentary assessment

Eligibility was checked across semantic case, pre/postconditions, outcome,
authority, replay/lifecycle/domain/version/order, migration branches, candidate
mapping identity, crypto/state/resource profiles, commands/corpus/outputs/review,
and evidence class. Any material mismatch prevents direct reuse; similarity is
insufficient. No current artifact qualified for an evidence-class upgrade.

## Future immutable manifest obligation

Before future results, bind source commit, governing blobs, common case, paired
manifest, both mappings and binding, harness/generator, commands, environment,
toolchain, corpus seed, outputs, summaries, reviewers, exclusions, evidence
class, and claim scope. A post-result manifest is audit metadata, not a
substitute for required pre-result registration.

## Package inventory repair

The current documentary receipt repairs package-level inventory provenance
only. It adds the previously omitted historical final dual-review file to the
governed inventory and separates the governed receipt schema from the excluded
executed receipt. This repair does not alter or upgrade candidate evidence,
historical verdicts, reuse eligibility, or Gate disposition.
