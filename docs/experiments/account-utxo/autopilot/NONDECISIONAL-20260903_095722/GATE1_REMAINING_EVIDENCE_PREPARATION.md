# Gate 1 — Remaining Ownership and Authorization Evidence Preparation

> **NON-NORMATIVE PREPARATION.** Gate-1 requirements are process-satisfied; this
> file neither supplies missing candidate evidence nor selects authorization.

## Frozen requirement and current satisfaction

The reviewed matrix requires both candidates to deterministically relate
control of native DLTH, accepted authorization evidence, the exact authorized
effect, applicable protocol/crypto versions, and relevant canonical state and
history. No privileged authority may override ordinary ownership; authorization
failure must have no partial canonical effect. Migration claims must state their
guarantee and assumptions, distinguish planned deprecation, distinguishable
compromise, and catastrophic indistinguishability, and name any independent
pre-existing information that makes recovery possible. Source: matrix §§5.1–5.5,
with `OWNERSHIP_AUTHORIZATION_REQUIREMENTS.md` and
`AUTHORIZATION_COVERAGE_MIGRATION_REQUIREMENTS.md`.

`G1-AUTH-001` has a reviewed common case, paired manifest, candidate mappings,
mapping identity binding, and focused reviews. A documentary positive structural
package exists. Its own manifest says durable complete-package provenance is
**PENDING EVIDENCE**, direct Gate-1 credit is **NOT ELIGIBLE**, and re-execution
is **REQUIRED**. No executable authorization validator, concrete artifact,
negative/failure campaign, migration campaign, formal proof, quantitative result,
or independent implementation is credited. The older pilot is incompatible in
case/profile/mapping identity and supplies procedure patterns only.

## Immediate provenance repair preparation

Before re-execution, prepare one immutable package manifest that binds:

- source commit; exact governing requirement/methodology blobs or hashes;
- common case, paired manifest, both mappings, mapping binding and review records;
- generator/harness identity, commands, environment, toolchain, and corpus seed;
- every candidate result, reviewer record, deterministic summary, and exclusion;
- evidence-class label and explicit claim scope.

The manifest must be frozen before results and must not rewrite old evidence.

## Required neutral branch suite

| Branch ID | Shared case variation | Required observations | Status |
|---|---|---|---|
| G1-N01 | malformed authorization evidence | bounded deterministic rejection; no canonical effect | **PENDING / REQUIRES IMPLEMENTATION** |
| G1-N02 | well-formed but invalid evidence | exact failure class; no partial effect | **PENDING / REQUIRES IMPLEMENTATION** |
| G1-N03 | evidence valid for a different effect/purpose/domain/version | scope failure without mechanism assumption | **PENDING / REQUIRES IMPLEMENTATION** |
| G1-N04 | missing, stale, or conflicting authorization state | dependency and outcome trace | **PENDING / REQUIRES IMPLEMENTATION** |
| G1-N05 | many expensive invalid artifacts | attempted-work trace and bounded staging | **PENDING / REQUIRES IMPLEMENTATION** |
| G1-M01 | planned deprecation; owner participation required | eligibility, handoff, current acceptance, history | **PENDING** |
| G1-M02 | planned deprecation; owner participation not assumed | achievable guarantee or explicit impossibility boundary | **PENDING** |
| G1-M03 | legacy currently accepted | current and historical interpretations separated | **PENDING** |
| G1-M04 | legacy currently rejected | dormant-value/state consequence without confiscation claim | **PENDING** |
| G1-M05 | retrofit information available before failure | identify exact independent prior information | **PENDING** |
| G1-M06 | no retrofit basis and no independent prior information | no recovery advantage claim | **PENDING** |
| G1-C01 | distinguishable credential compromise | competing legitimate/forged effects under declared authority facts | **PENDING** |
| G1-C02 | catastrophic forgeability with distinguishability | state only evidence-supported guarantee | **PENDING** |
| G1-C03 | catastrophic forgeability without distinguishability | record information-theoretic limitation symmetrically | **PENDING** |
| G1-H01 | historical validation after retirement | version-relative acceptance and interpretation | **PENDING** |
| G1-R01 | rollback then reapplication | restore authorization/replay facts before re-evaluation | **PENDING** |

For every migration branch record: guarantee level; owner participation;
current-acceptance policy; dormant value/state treatment; historical-validation
policy; retrofit availability; independent prior distinguishing information;
protocol/crypto version; and whether the outcome is success, rejection, or an
explicitly defined accepted-unsuccessful class.

## Tests possible now

Document lint can verify exact hashes, paired field equality, exclusions,
candidate-symmetric scenario presence, no candidate-native scoring, and that
every negative branch has an external oracle. Paper traces can check arithmetic
and declared dependency consistency. These are structural checks only.

## Symmetry and stop points

Both mappings receive the same external `P0/V/E1/A1/P1/M1` contract and branch
profile. Internal realizations may differ and must be disclosed separately.
Stop before choosing credentials, signing bytes, address/identity structure,
crypto/PQ primitive, grouping, recovery, migration mechanism, replay mechanism,
or current-acceptance production policy: **REQUIRES OWNER DECISION**.

Comparison scoring: **NOT STARTED**. Ranking: **NONE**. Decision: **NOT MADE**.
Account selected: **NO**. UTXO selected: **NO**. Main merge: **NOT DONE**.
